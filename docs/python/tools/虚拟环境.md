# 虚拟环境

[官方文档](https://docs.python.org/zh-cn/3/library/venv.html)

虚拟环境用于隔离不同 Python 项目的依赖，避免包版本冲突。本文将介绍如何创建和管理虚拟环境。

## venv 模块

### 创建虚拟环境

```bash
# 创建虚拟环境
python -m venv myenv

# 激活（Windows）
myenv\Scripts\activate

# 激活（macOS/Linux）
source myenv/bin/activate

# 退出
deactivate
```

### 使用虚拟环境

```bash
# 激活后安装包
pip install requests

# 查看已安装的包
pip list

# 导出依赖
pip freeze > requirements.txt

# 从文件安装
pip install -r requirements.txt
```

## virtualenv

virtualenv 是更强大的虚拟环境工具。

```bash
pip install virtualenv

# 创建
virtualenv myenv

# 指定 Python 版本
virtualenv -p python3.9 myenv
```

## conda 环境

```bash
# 创建环境
conda create -n myenv python=3.9

# 激活
conda activate myenv

# 退出
conda deactivate

# 删除
conda remove -n myenv --all
```

## 最佳实践

1. **每个项目一个环境**：避免依赖冲突
2. **记录依赖**：使用 requirements.txt
3. **忽略环境目录**：添加到 .gitignore
4. **命名规范**：使用项目名或 venv
5. **定期更新**：保持包版本最新

## 总结

本文介绍了 Python 虚拟环境：

- ✅ venv 模块
- ✅ virtualenv
- ✅ conda 环境
- ✅ 最佳实践

掌握虚拟环境后，你可以继续学习 [代码规范](/python/tools/代码规范)。
