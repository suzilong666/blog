# 模型微调基础

## 什么是模型微调？

模型微调（Fine-tuning）是指在预训练大模型的基础上，使用特定领域的数据进行进一步训练，使模型更好地适应特定任务或领域。

### 为什么需要微调？

**预训练模型的局限性：**
- 通用性强但专业性不足
- 缺乏特定领域的知识
- 风格不符合企业需求
- 某些任务效果不佳

**微调的优势：**
- ✅ 提升特定任务性能
- ✅ 注入领域知识
- ✅ 定制化输出风格
- ✅ 减少 Prompt 长度
- ✅ 降低推理成本

### 微调 vs Prompt Engineering

| 特性 | Prompt Engineering | 微调 |
|------|-------------------|------|
| 实现难度 | 低 | 中到高 |
| 数据需求 | 无 | 需要标注数据 |
| 成本 | 低 | 中高 |
| 效果提升 | 有限 | 显著 |
| 适用场景 | 快速验证 | 生产环境 |

## 微调方法概述

### 1. 全量微调（Full Fine-tuning）

更新模型的所有参数。

**优点：**
- 效果最好
- 完全适配目标任务

**缺点：**
- 计算资源需求巨大
- 容易过拟合
- 存储成本高

```python
# 伪代码示例
model = AutoModelForCausalLM.from_pretrained("llama-3-8b")

# 解冻所有参数
for param in model.parameters():
    param.requires_grad = True

# 训练
trainer = Trainer(
    model=model,
    train_dataset=train_data,
    args=TrainingArguments(...)
)
trainer.train()
```

### 2. LoRA（Low-Rank Adaptation）

只训练低秩分解矩阵，冻结原始模型参数。

**优点：**
- 参数量少（约 1%）
- 训练速度快
- 易于切换不同任务
- 存储成本低

**缺点：**
- 效果略低于全量微调

```python
from peft import LoraConfig, get_peft_model

# 配置 LoRA
lora_config = LoraConfig(
    r=16,  # 秩
    lora_alpha=32,
    target_modules=["q_proj", "v_proj"],  # 目标层
    lora_dropout=0.05,
    bias="none"
)

# 应用 LoRA
model = AutoModelForCausalLM.from_pretrained("llama-3-8b")
model = get_peft_model(model, lora_config)

# 训练（只训练 LoRA 参数）
trainer = Trainer(model=model, ...)
```

### 3. QLoRA（Quantized LoRA）

结合量化和 LoRA，进一步降低资源需求。

**特点：**
- 4-bit 量化 + LoRA
- 可在消费级 GPU 上训练
- 内存占用极低

```python
from bitsandbytes import BitsAndBytesConfig

# 4-bit 量化配置
bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_quant_type="nf4",
    bnb_4bit_compute_dtype=torch.float16
)

# 加载量化模型
model = AutoModelForCausalLM.from_pretrained(
    "llama-3-8b",
    quantization_config=bnb_config
)

# 应用 LoRA
model = get_peft_model(model, lora_config)
```

## 何时需要微调？

### 适合微调的场景

✅ **需要微调：**
1. 专业领域问答（医疗、法律、金融）
2. 特定风格的文本生成
3. 结构化输出（JSON、SQL）
4. 企业内部术语和流程
5. 高频使用的固定任务

❌ **不需要微调：**
1. 通用问答
2. 临时性任务
3. 数据量太少（<100 条）
4. Prompt Engineering 已能满足需求

## 数据准备

### 数据格式

OpenAI 微调数据格式：

```jsonl
{"messages": [{"role": "system", "content": "你是客服助手"}, {"role": "user", "content": "如何退货？"}, {"role": "assistant", "content": "您可以在订单页面申请退货..."}]}
{"messages": [{"role": "user", "content": "产品保修期多久？"}, {"role": "assistant", "content": "我们的产品提供一年保修服务。"}]}
```

### 数据收集

```python
import json

# 从对话日志构建数据集
def build_dataset(conversations: list) -> list:
    """将对话转换为微调格式"""
    dataset = []
    
    for conv in conversations:
        messages = [
            {"role": "system", "content": "你是专业的客服助手"}
        ]
        
        for turn in conv["turns"]:
            messages.append({
                "role": "user" if turn["sender"] == "user" else "assistant",
                "content": turn["message"]
            })
        
        dataset.append({"messages": messages})
    
    return dataset

# 保存为 JSONL
def save_jsonl(data: list, filename: str):
    with open(filename, 'w', encoding='utf-8') as f:
        for item in data:
            f.write(json.dumps(item, ensure_ascii=False) + '\n')

# 使用
dataset = build_dataset(conversations)
save_jsonl(dataset, "training_data.jsonl")
```

### 数据质量要求

**最少数据量：**
- GPT-3.5-turbo：至少 50-100 条
- 建议：500+ 条以获得好效果

**质量标准：**
1. 答案准确无误
2. 风格一致
3. 覆盖常见场景
4. 包含边界案例

## OpenAI 微调实战

### 1. 安装 SDK

```bash
pip install openai
```

### 2. 上传训练数据

```python
from openai import OpenAI

client = OpenAI()

# 上传文件
file = client.files.create(
    file=open("training_data.jsonl", "rb"),
    purpose="fine-tune"
)

print(f"文件 ID: {file.id}")
```

### 3. 创建微调任务

```python
# 创建微调作业
job = client.fine_tuning.jobs.create(
    training_file=file.id,
    model="gpt-3.5-turbo",
    hyperparameters={
        "n_epochs": 3,  # 训练轮数
        "batch_size": 4,
        "learning_rate_multiplier": 0.1
    }
)

print(f"作业 ID: {job.id}")
print(f"状态: {job.status}")
```

### 4. 监控训练进度

```python
# 查看作业状态
job = client.fine_tuning.jobs.retrieve(job.id)
print(f"状态: {job.status}")
print(f"已完成步数: {job.trained_tokens}")

# 列出事件
events = client.fine_tuning.jobs.list_events(job.id)
for event in events.data:
    print(f"{event.created_at}: {event.message}")
```

### 5. 使用微调模型

```python
# 训练完成后使用
response = client.chat.completions.create(
    model="ft:gpt-3.5-turbo:my-org:custom_suffix:id",
    messages=[
        {"role": "user", "content": "如何退货？"}
    ]
)

print(response.choices[0].message.content)
```

## Hugging Face 微调实战

### 使用 Transformers

```python
from transformers import (
    AutoTokenizer,
    AutoModelForCausalLM,
    TrainingArguments,
    Trainer
)
from datasets import load_dataset
from peft import LoraConfig, get_peft_model

# 1. 加载数据和模型
dataset = load_dataset("json", data_files="training_data.jsonl")
model_name = "meta-llama/Llama-3-8b"

tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForCausalLM.from_pretrained(model_name)

# 2. 配置 LoRA
lora_config = LoraConfig(
    r=16,
    lora_alpha=32,
    target_modules=["q_proj", "v_proj"],
    lora_dropout=0.05
)

model = get_peft_model(model, lora_config)

# 3. 数据预处理
def tokenize_function(examples):
    return tokenizer(
        examples["text"],
        truncation=True,
        max_length=512,
        padding="max_length"
    )

tokenized_dataset = dataset.map(tokenize_function, batched=True)

# 4. 训练配置
training_args = TrainingArguments(
    output_dir="./results",
    num_train_epochs=3,
    per_device_train_batch_size=4,
    gradient_accumulation_steps=4,
    learning_rate=2e-4,
    fp16=True,
    logging_steps=10,
    save_strategy="epoch"
)

# 5. 开始训练
trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=tokenized_dataset["train"]
)

trainer.train()

# 6. 保存模型
model.save_pretrained("./fine_tuned_model")
tokenizer.save_pretrained("./fine_tuned_model")
```

### 使用 Unsloth（更快更简单）

```bash
pip install unsloth
```

```python
from unsloth import FastLanguageModel

# 加载模型（自动优化）
model, tokenizer = FastLanguageModel.from_pretrained(
    model_name="unsloth/llama-3-8b",
    max_seq_length=2048,
    load_in_4bit=True  # QLoRA
)

# 添加 LoRA
model = FastLanguageModel.get_peft_model(
    model,
    r=16,
    target_modules=["q_proj", "k_proj", "v_proj", "o_proj"],
    lora_alpha=16,
    lora_dropout=0
)

# 训练
from trl import SFTTrainer
from transformers import TrainingArguments

trainer = SFTTrainer(
    model=model,
    train_dataset=dataset,
    dataset_text_field="text",
    max_seq_length=2048,
    args=TrainingArguments(
        per_device_train_batch_size=2,
        gradient_accumulation_steps=4,
        warmup_steps=5,
        max_steps=60,
        learning_rate=2e-4,
        fp16=True,
        logging_steps=1,
        output_dir="outputs"
    )
)

trainer.train()
```

## 评估微调效果

### 1. 人工评估

```python
def evaluate_model(model, test_questions: list) -> dict:
    """人工评估模型表现"""
    
    results = []
    
    for question in test_questions:
        # 获取模型回答
        response = generate_response(model, question)
        
        # 人工评分（1-5分）
        print(f"问题: {question}")
        print(f"回答: {response}")
        score = int(input("评分 (1-5): "))
        
        results.append({
            "question": question,
            "response": response,
            "score": score
        })
    
    avg_score = sum(r["score"] for r in results) / len(results)
    
    return {
        "average_score": avg_score,
        "results": results
    }
```

### 2. 自动指标

```python
from rouge_score import rouge_scorer

def calculate_rouge(predictions: list, references: list) -> dict:
    """计算 ROUGE 分数"""
    
    scorer = rouge_scorer.RougeScorer(['rouge1', 'rouge2', 'rougeL'], use_stemmer=True)
    
    scores = {
        "rouge1": [],
        "rouge2": [],
        "rougeL": []
    }
    
    for pred, ref in zip(predictions, references):
        score = scorer.score(ref, pred)
        for key in scores:
            scores[key].append(score[key].fmeasure)
    
    # 平均分
    return {key: sum(val)/len(val) for key, val in scores.items()}
```

### 3. A/B 测试

```python
# 对比微调前后效果
def ab_test(question: str) -> dict:
    """A/B 测试"""
    
    # 原始模型
    original_response = original_model.generate(question)
    
    # 微调模型
    finetuned_response = finetuned_model.generate(question)
    
    return {
        "question": question,
        "original": original_response,
        "finetuned": finetuned_response
    }
```

## 最佳实践

### 1. 数据准备

```python
# ✅ 好的训练数据
{
    "messages": [
        {"role": "system", "content": "你是专业的Python编程助手"},
        {"role": "user", "content": "如何实现单例模式？"},
        {"role": "assistant", "content": "在Python中，可以使用以下方法实现单例模式：\n\n```python\nclass Singleton:\n    _instance = None\n    \n    def __new__(cls):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n```\n\n这种方法使用 `__new__` 方法控制实例创建。"}
    ]
}

# ❌ 糟糕的训练数据
# - 回答太短
# - 缺少细节
# - 格式不一致
```

### 2. 超参数调优

```python
# 推荐的起始配置
hyperparameters = {
    "n_epochs": 3,           # 从小开始，逐步增加
    "batch_size": 4,         # 根据显存调整
    "learning_rate": 2e-5,   # 较小的学习率
    "weight_decay": 0.01     # 防止过拟合
}
```

### 3. 防止过拟合

- 使用验证集监控
- 早停策略
- 正则化（weight decay）
- 数据增强

### 4. 成本控制

```python
# OpenAI 微调成本估算
def estimate_finetune_cost(num_tokens: int, n_epochs: int = 3) -> float:
    """估算微调成本"""
    # GPT-3.5-turbo: $0.008 / 1K tokens
    cost_per_1k = 0.008
    
    total_tokens = num_tokens * n_epochs
    cost = (total_tokens / 1000) * cost_per_1k
    
    return round(cost, 2)

# 示例：10万 tokens，3个 epoch
cost = estimate_finetune_cost(100000, 3)
print(f"预计成本: ${cost}")  # $2.40
```

## 常见问题

### Q1: 需要多少数据？

- **最少**：50-100 条高质量样本
- **推荐**：500-1000 条
- **理想**：2000+ 条

### Q2: 微调后效果不好怎么办？

1. 检查数据质量
2. 增加数据量
3. 调整超参数
4. 尝试不同的基础模型

### Q3: 如何管理多个微调模型？

```python
# 为不同任务使用不同的模型
models = {
    "customer_service": "ft:gpt-3.5-turbo:cs-v1",
    "code_assistant": "ft:gpt-3.5-turbo:code-v1",
    "data_analysis": "ft:gpt-3.5-turbo:data-v1"
}

def get_model(task: str):
    return models.get(task, "gpt-3.5-turbo")
```

## 小结

本章我们学习了：
- ✅ 微调的概念和优势
- ✅ 主流微调方法（LoRA、QLoRA）
- ✅ OpenAI 和 Hugging Face 微调实战
- ✅ 数据准备和评估
- ✅ 最佳实践和常见问题

微调是提升模型性能的重要手段，但需要权衡成本和收益。

## 延伸阅读

- [OpenAI Fine-tuning Guide](https://platform.openai.com/docs/guides/fine-tuning)
- [LoRA Paper](https://arxiv.org/abs/2106.09685)
- [Hugging Face PEFT Documentation](https://huggingface.co/docs/peft)
