# Prompt 工程基础

## 什么是 Prompt

Prompt（提示词）是你输入给大模型的文本指令，用于引导模型生成期望的输出。可以把 Prompt 理解为与 AI 对话的"问题"或"指令"，它的质量直接决定了回答的质量。

> **核心理念**：Garbage In, Garbage Out（垃圾进，垃圾出）
> 
> 糟糕的 Prompt 产生糟糕的结果，优秀的 Prompt 产生优秀的结果。

## Prompt 的重要性

### 为什么需要精心设计的 Prompt？

1. **消除歧义**：明确的指令减少误解
2. **控制输出**：指定格式、风格、长度
3. **提升质量**：引导模型发挥最佳能力
4. **降低成本**：减少试错和重复调用

### 对比示例

❌ **糟糕的 Prompt：**
```
写点关于人工智能的东西
```

✅ **优秀的 Prompt：**
```
请以通俗易懂的语言，向完全没有技术背景的普通人解释什么是人工智能。
要求：
- 使用生活中的类比
- 避免专业术语
- 控制在 300 字以内
- 包含一个具体的应用场景
```

## Prompt 的基本结构

一个完整的 Prompt 通常包含以下要素：

```
┌─────────────────────────────────────┐
│         Prompt 结构框架              │
├─────────────────────────────────────┤
│ 1. 角色设定 (Role)                   │
│ 2. 任务描述 (Task)                   │
│ 3. 上下文背景 (Context)              │
│ 4. 约束条件 (Constraints)            │
│ 5. 输出格式 (Format)                 │
│ 6. 示例参考 (Examples) [可选]        │
└─────────────────────────────────────┘
```

### 1. 角色设定（Role）

为模型指定一个特定的角色或身份，帮助它调整回答的风格和专业度。

```markdown
你是一位经验丰富的 Python 开发工程师...
你是一名资深产品经理...
你是一个专业的翻译专家...
```

**示例：**
```
你是一位有10年教学经验的高中数学老师，擅长用生动有趣的方式讲解复杂的数学概念。
```

### 2. 任务描述（Task）

清晰说明你希望模型完成的具体任务。

```markdown
请帮我...
你需要...
任务是...
```

**示例：**
```
请帮我分析这段 Python 代码的性能瓶颈，并提出优化建议。
```

### 3. 上下文背景（Context）

提供必要的背景信息，帮助模型更好地理解任务。

```markdown
背景信息：...
当前情况：...
已知条件：...
```

**示例：**
```
我正在开发一个电商网站的用户注册功能，使用的是 Django 框架。
```

### 4. 约束条件（Constraints）

明确限制和边界条件。

```markdown
要求：...
注意：...
不要...
必须...
```

**示例：**
```
要求：
- 代码必须符合 PEP 8 规范
- 需要添加详细的注释
- 考虑边界情况和异常处理
- 不要使用第三方库
```

### 5. 输出格式（Format）

指定期望的输出形式。

```markdown
请以...格式输出
输出应为...
使用表格展示...
```

**示例：**
```
请以 JSON 格式输出结果，包含以下字段：name, age, email
```

### 6. 示例参考（Examples）[可选]

提供参考示例，帮助模型理解期望的输出。

```markdown
例如：...
参考以下格式：...
像这样：...
```

## 常用 Prompt 技巧

### 1. Zero-Shot Prompting（零样本提示）

不提供示例，直接让模型完成任务。适用于模型已经充分学习的常见任务。

```markdown
将以下英文翻译成中文：
"Artificial intelligence is transforming the world."
```

### 2. Few-Shot Prompting（少样本提示）

提供少量示例，帮助模型理解任务模式。

```markdown
根据以下示例，将情感分类为"正面"、"负面"或"中性"：

示例1：
输入："这部电影太棒了！"
输出：正面

示例2：
输入："服务很差，不会再来了。"
输出：负面

示例3：
输入："今天天气还可以。"
输出：中性

现在请分类：
输入："产品质量一般，价格偏贵。"
输出：
```

### 3. Chain-of-Thought（思维链）

引导模型逐步推理，特别适用于复杂问题。

```markdown
请逐步思考并解答：

问题：小明有5个苹果，他吃了2个，又买了3个，现在有多少个？

解答步骤：
1. 初始数量：5个
2. 吃了2个：5 - 2 = 3个
3. 又买了3个：3 + 3 = 6个
4. 最终答案：6个

请用同样的方式解答：
问题：一个班级有30名学生，其中60%是女生，男生有多少人？
```

**效果对比：**

❌ **不使用思维链：**
```
答案：12人
```

✅ **使用思维链：**
```
解答步骤：
1. 总人数：30人
2. 女生比例：60%
3. 男生比例：100% - 60% = 40%
4. 男生人数：30 × 40% = 12人
5. 最终答案：12人
```

### 4. Role Prompting（角色扮演）

赋予模型特定角色，获得更专业的回答。

```markdown
你是一位资深软件架构师，正在审查 junior 开发者的代码。
请指出以下代码的问题，并给出改进建议：

[代码内容]
```

### 5. Template Prompting（模板化）

创建可复用的 Prompt 模板。

```markdown
# 代码审查模板

## 代码功能
{code_description}

## 代码内容
<!-- 代码语言：{language} -->
{code}

## 审查要求
- 检查潜在 bug
- 评估性能问题
- 提出改进建议
- 评分（1-10分）

## 审查结果
```

### 6. Negative Prompting（负向提示）

明确告诉模型不要做什么。

```markdown
请总结这篇文章的主要观点。

要求：
- 简洁明了，不超过100字
- 不要包含个人观点
- 不要引用原文
- 不要添加额外信息
```

## 最佳实践

### ✅ DO - 应该做的

1. **具体明确**
   ```markdown
   ✅ 请列出5个最受欢迎的Python Web框架，并简要说明各自特点
   ❌ 说说Python框架
   ```

2. **结构化表达**
   ```markdown
   ✅ 使用列表、分段、标记来组织Prompt
   ❌ 一大段文字没有结构
   ```

3. **提供上下文**
   ```markdown
   ✅ 我是编程初学者，刚学了2周Python，请解释什么是装饰器
   ❌ 解释装饰器
   ```

4. **指定期望格式**
   ```markdown
   ✅ 请用表格形式对比，包含列：框架名、学习难度、适用场景
   ❌ 对比一下这些框架
   ```

5. **迭代优化**
   - 第一次结果不满意时，调整 Prompt 重试
   - 记录成功的 Prompt 模式

### ❌ DON'T - 不应该做的

1. **避免模糊不清**
   ```markdown
   ❌ 给我一些好的想法
   ✅ 请为一家咖啡馆提供5个营销创意
   ```

2. **避免过度复杂**
   ```markdown
   ❌ 一个包含几十个要求的超长Prompt
   ✅ 分解为多个简单的Prompt，逐步完成
   ```

3. **避免矛盾指令**
   ```markdown
   ❌ 详细解释但要非常简短
   ✅ 明确优先级：简洁为主，关键点展开
   ```

4. **不要假设模型知道**
   ```markdown
   ❌ 按照我之前说的那个方法做
   ✅ 重新描述完整要求
   ```

## 常见错误与避坑指南

### 错误1：指令不够具体

❌ **问题：**
```
帮我写代码
```

✅ **改进：**
```
请用Python编写一个函数，接收一个列表作为参数，返回去重后的列表，保持原有顺序。
要求：
- 时间复杂度 O(n)
- 添加类型注解
- 包含docstring和使用示例
```

### 错误2：忽略上下文

❌ **问题：**
```
这个怎么改？
```

✅ **改进：**
```
我在开发一个用户登录系统，遇到以下问题：
- 技术栈：React + Node.js + MongoDB
- 问题：密码验证总是失败
- 已尝试：确认了密码哈希算法一致

以下是相关代码：
[代码内容]

请帮我找出问题所在。
```

### 错误3：一次性要求太多

❌ **问题：**
```
创建一个完整的电商网站，包括前端、后端、数据库、支付集成、用户认证...
```

✅ **改进：**
```
第一步：请帮我设计电商网站的用户数据模型，包含以下功能：
- 用户注册登录
- 个人信息管理
- 收货地址管理

请使用 MongoDB schema 格式输出。
```

### 错误4：没有指定输出格式

❌ **问题：**
```
比较一下React和Vue
```

✅ **改进：**
```
请对比 React 和 Vue 框架，以表格形式呈现，包含以下维度：
- 学习曲线
- 社区规模
- 性能表现
- 适用场景
- 代表公司
```

### 错误5：忽视迭代优化

❌ **做法：**
- 第一次结果不好就放弃
- 认为模型能力有限

✅ **做法：**
- 分析哪里不符合预期
- 调整 Prompt 重新尝试
- 积累成功的 Prompt 模式

## 实用 Prompt 模板

### 模板1：代码生成

```text
角色：你是一位资深的 [编程语言] 开发工程师
任务：编写一个函数，实现 [功能描述]
要求：
- 输入：[输入描述]
- 输出：[输出描述]
- 边界情况：[边界情况]
- 性能要求：[性能要求]

输出格式：
- 完整代码
- 代码注释
- 使用示例
- 时间/空间复杂度分析
```

### 模板2：代码审查

```text
角色：你是一位严格的代码审查员
待审查代码：[代码内容]

审查维度：
1. 正确性：是否有bug或逻辑错误
2. 性能：是否存在性能瓶颈
3. 可读性：代码是否清晰易懂
4. 安全性：是否有安全隐患
5. 规范性：是否符合最佳实践

输出格式：
- 问题列表（按严重程度排序）
- 改进建议
- 优化后的代码
- 综合评分（1-10）
```

### 模板3：学习解释

```text
角色：你是一位善于教学的 [学科] 老师
学生背景：[学生背景描述]
要解释的概念：[概念名称]

教学要求：
- 使用生活化的类比
- 避免专业术语，或使用时立即解释
- 由浅入深，循序渐进
- 包含具体例子
- 最后给出总结

输出长度：[长度要求]
```

### 模板4：文档生成

```text
任务：为以下代码生成技术文档
代码：[代码内容]

文档结构：
1. 功能概述
2. API 接口说明
3. 参数详解
4. 返回值说明
5. 使用示例（至少3个）
6. 注意事项
7. 常见问题

风格要求：
- 专业但易懂
- 适合中级开发者阅读
```

## 高级技巧

### 1. 分隔符的使用

使用特殊符号清晰分隔不同部分：

```markdown
请分析以下代码的问题：

=== 代码开始 ===
def calculate_sum(numbers):
    total = 0
    for num in numbers:
        total += num
    return total
=== 代码结束 ===

请从以下角度分析：
1. 代码风格
2. 性能优化
3. 边界处理
```

常用分隔符：
- `===` 或 `---`
- `"""` 或 `'''`
- XML 标签：`<code>...</code>`
- Markdown 代码块

### 2. 引导性问题

通过提问引导模型思考方向：

```markdown
在优化这段代码之前，请先思考：
1. 当前的时间复杂度是多少？
2. 哪些操作是最耗时的？
3. 有没有重复计算的部分？
4. 可以使用什么数据结构优化？

然后再给出优化方案。
```

### 3. 自我验证

要求模型检查自己的回答：

```markdown
请解答这个问题，然后：
1. 验证你的答案是否正确
2. 检查是否有遗漏的情况
3. 如果有错误，请修正
4. 给出最终确定的答案
```

### 4. 元 Prompt（Meta-Prompt）

让模型帮你优化 Prompt：

```markdown
我想让AI帮我审查代码，但我不知道如何写一个好的Prompt。

请帮我设计一个代码审查的Prompt模板，要求：
- 适用于多种编程语言
- 能发现常见的问题
- 输出结构清晰
- 可以重复使用
```

## 实战练习

### 练习1：改进以下 Prompt

原始 Prompt：
```
解释一下递归
```

改进后的 Prompt：
<details>
<summary>点击查看答案</summary>

```markdown
你是一位计算机科学老师，正在给大一新生讲解递归概念。

要求：
1. 从生活中的例子引入（如俄罗斯套娃）
2. 解释递归的核心思想
3. 给出一个简单的代码示例（Python）
4. 说明递归的两个必要条件
5. 指出常见的陷阱
6. 控制在500字以内
```
</details>

### 练习2：设计一个 Prompt

场景：你需要让 AI 帮你将一个 Java 类转换为 Python 类。

<details>
<summary>查看参考方案</summary>

```markdown
# 角色
你是一位精通 Java 和 Python 的全栈工程师

# 任务
将以下 Java 类转换为等效的 Python 类

# Java 代码
```java
[java_code_here]
```

# 转换要求
1. 保持相同的功能和接口
2. 使用 Pythonic 的写法
3. 添加类型注解
4. 添加 docstring
5. 指出两种语言的主要差异

# 输出格式
- Python 代码
- 差异说明
- 注意事项
```
</details>

## 工具推荐

### Prompt 管理工具

- **Prompt Library**：收集和整理优质 Prompt
- **LangChain Hub**：分享和复用 Prompt 模板
- **OpenAI Playground**：测试和优化 Prompt

### 辅助工具

- **Token Counter**：计算 Prompt 的 token 数量
- **Prompt Perfect**：自动优化 Prompt
- **FlowGPT**：Prompt 社区和 marketplace

## 小结

Prompt Engineering 是与大模型高效沟通的核心技能。掌握以下要点：

✅ **核心原则**
- 具体明确，避免模糊
- 结构清晰，层次分明
- 提供上下文和示例
- 迭代优化，持续改进

✅ **常用技巧**
- Zero-shot / Few-shot prompting
- Chain-of-thought reasoning
- Role prompting
- Template-based prompting

✅ **避免陷阱**
- 指令不具体
- 缺少上下文
- 要求过多过杂
- 不指定输出格式

下一节我们将学习如何**搭建开发环境**，开始实际的 API 调用和代码开发。

## 延伸阅读

- [OpenAI Prompt Engineering Guide](https://platform.openai.com/docs/guides/prompt-engineering)
- [Anthropic Prompt Engineering](https://docs.anthropic.com/claude/docs/intro-to-prompting)
- [Learn Prompting](https://learnprompting.org/)
